/** Maximum file size accepted for a custom card reveal sound (bytes). */
export const AUDIO_UPLOAD_MAX_BYTES = 1 * 1024 * 1024; // 1 MB

/** Maximum duration for a custom card reveal sound (seconds). */
export const AUDIO_UPLOAD_MAX_DURATION_S = 10;
